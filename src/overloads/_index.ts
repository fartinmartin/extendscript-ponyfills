export { includes } from "./includes";

// TODO: in addition to overload, it might be nice to be able to access the conflicting names via their namespace, ie:
// array.includes and string.includes (instead of array.arrayIncludes/string.stringIncludes)
